// WiClub's franchise royalty: a fixed 12% of revenue, the same for every
// club in the network (confirmed by Anastasiia — not per-club, not tiered).
// If this ever changes, or starts varying by club, this needs to become a
// per-partner field instead of a constant.
export const ROYALTY_PERCENT = 12;
